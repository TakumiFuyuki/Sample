# cloudbuild.yaml

steps:
# Python アプリケーションのビルド
- name: 'python:3.9'
  entrypoint: 'bash'
  args:
  - '-c'
  - |
    pip install -r requirements.txt
    # その他のビルド手順（必要に応じて追加）
    gcloud app deploy app.yaml --quiet
